# v0.5 — 面向普通用户的轻量公开版

## 新功能

- 首次运行自动检查 `yt-dlp.exe`、`ffmpeg.exe`、`ffprobe.exe` 和 `deno.exe`。
- 缺少组件时，可直接在图形界面中从官方来源下载安装。
- 每条下载线路失败后自动重试，并切换可信备用线路。
- 安装前使用发布方提供的 SHA-256 清单验证文件；校验失败不会安装。
- 安装中断后可继续，已成功安装的组件不会重复下载。
- “更新 yt-dlp 核心”改为“检查运行组件”；组件齐全时仍可更新 yt-dlp。
- 安装缓存和 yt-dlp 运行临时文件改放在便携程序目录，降低系统临时盘空间不足造成的失败。

## 保留功能

- 自动从整段分享文案中提取第一个视频网址。
- MP4、MP3、SRT 分别保存到 `mp4`、`mp3`、`subtitles` 子目录。
- 只请求所选的一种字幕语言，并将已下载的 VTT 转为 SRT。
- Firefox Cookies 下载结束后自动取消勾选。

## 安装说明

1. 下载并完整解压 `yt-dlp-gui-v0.5-windows-x64.zip`。
2. 双击 `yt-dlp-gui.exe`。
3. 首次运行按提示下载组件，完整下载约 250 MB。

本项目不是 yt-dlp 官方产品。请仅下载你有权访问和使用的内容。站点规则、权限、DRM 和限流可能导致下载失败。
